
% Text for starting the sim, if verbose is turned on
programStyle = 'green';
textStyle = '*green';
if config.general.verbose
        cprintf(programStyle,'[DCAM]\t')
        cprintf(textStyle, 'Starting Simulation\n\n')
end
